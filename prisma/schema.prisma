// *** GENERATED BY PRISMIX :: DO NOT EDIT ***
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
  output   = "./prisma/generated/prisma-client-js"
}

model Role {
  id        String   @id @default(uuid())
  name      String
  type      RoleType @default(MANAGER)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  user      User?    @relation(name: "RoleToUser")
}

model User {
  id            String   @id @unique @default(uuid())
  role          Role     @relation(name: "RoleToUser", fields: [roleId], references: [id])
  roleId        String   @unique
  firstName     String
  middleName    String
  lastName      String
  isEnabled     Boolean
  isDeleted     Boolean
  is2faEnabled  Boolean
  clientId      String
  email         String   @unique
  emailVerified Boolean
  phoneNumber   String   @unique
  phoneVerified Boolean
  createdBy     String
  modifiedBy    String
  saltKey       String
  secretHash    String
  createdOn     DateTime @default(now())
  modifiedOn    DateTime @updatedAt

  @@index([firstName, lastName, email])
}

model VehicleCategory {
  id           String        @id @unique @default(uuid())
  categoryName String
  createdBy    String
  modifiedBy   String
  createdOn    DateTime      @default(now())
  modifiedOn   DateTime      @updatedAt
  vehicleType  VehicleTypes? @relation(name: "VehicleCategoryToVehicleTypes")

  @@index([categoryName])
}

model VehicleTypes {
  id             String          @id @unique @default(uuid())
  typeName       String
  category       VehicleCategory @relation(name: "VehicleCategoryToVehicleTypes", fields: [categoryId], references: [id])
  categoryId     String          @unique
  imageUrl       String
  adultSeatCount Int
  childSeatCount Int
  luggageCount   Int
  createdBy      String
  modifiedBy     String
  createdOn      DateTime        @default(now())
  modifiedOn     DateTime        @updatedAt

  @@index([typeName])
}

model Tariff {
  id String @id @unique @default(uuid())
}

enum RoleType {
  MASTER
  MANAGER
  ADMIN
}
