generator client {
  provider = "prisma-client-js"
  previewFeatures = []
  output   = "./prisma/generated/prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
